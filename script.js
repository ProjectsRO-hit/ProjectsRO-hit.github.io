const header = document.querySelector('header');
const body = document.querySelector('main');
const footer = document.querySelector('footer');

header.style.minHeight = `${window.innerHeight * 0.1}px`;
body.style.minHeight = `${window.innerHeight * 0.83}px`;
footer.style.minHeight = `${window.innerHeight * 0.07}px`;
